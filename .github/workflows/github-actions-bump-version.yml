name: Bump package version
on: workflow_dispatch
permissions:
  contents: write
jobs:
  bump:
    name: Bump package version
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repo
      uses: actions/checkout@v4

    - name: Bumping version
      uses: jpb06/bump-package@v2.5.15
